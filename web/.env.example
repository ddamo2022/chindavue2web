# Base URL for backend requests. Use the production API gateway unless you have a staging override.
VITE_API_BASE=https://www.vdamo.com

# Default locale for the desktop client (optional).
# VITE_DEFAULT_LOCALE=en
